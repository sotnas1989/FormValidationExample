function formValidation() // In this function we can make more complex validations, but for the example is not needed
{      
   // Accessing data by DOM an doing complex validations
    
}

document.getElementById("btonAdd").addEventListener("click", formValidation, false);